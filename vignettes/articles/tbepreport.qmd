---
title: "Get Started with tbepreport"
format: html
---

```{r}
#| include: false
knitr::opts_chunk$set(collapse = TRUE, comment = "#>", warning = FALSE, message = FALSE)
```

This article is a placeholder for the `tbepreport` package walkthrough - a
shortened version of the working prototype at `docs/exploratory.qmd`. It
will be filled in as the indicator functions listed in the package's
development plan are ported.

```{r}
library(tbepreport)
```
